# xmlwriter 1.1.9000

- added argument `use_prolog` to `list_as_xml_string` that allows for generating
the XML prolog or not. Default is `FALSE` to maintain backwards compatibility. 
Thanks for @guidovandenheuvel for reporting.

